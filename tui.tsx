/**
 * Package-root TUI entry shim. The host discovers the TUI entry as a `tui.tsx`
 * file at the package root; the implementation lives in `./src/tui.tsx`.
 */
export { default } from "./src/tui.tsx";
